
Instructions For use

# Build openwrt as normal
cd openwrt
make menuconfig
make

# Build qemu
cd ..
cd qemu
./configure
make

# Build hacked seabios (we don't use the Qemu bios because it's 128k).
cd seabios
make
cd ..

# Finally to run the openwrt image in the emulator
./run.sh

